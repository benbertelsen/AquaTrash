//GAME CLASS

class Game {
    constructor() {
        this.player1 = {};
        this.floatingTrash = [];
        this.score = 0;
        this.floatingTrashFrequency = 0; //same things as the frames in the other games we have created
        // this.animationID = null; //NB: we'll see this later. We don't have any animation, before the game starts.
        // this.gameOver = false;
    }
}